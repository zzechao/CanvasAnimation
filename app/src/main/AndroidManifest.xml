<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.base.canvasanimation">

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/xin"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/xin"
        android:supportsRtl="true"
        android:theme="@style/Theme.CanvasAnimation">
        <activity
            android:name="com.base.canvasanimation.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.CanvasAnimation.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <!--suppress AndroidElementNotAllowed -->
    <meta-data
        android:name="SvnBuildVersion"
        android:value="0" />
</manifest>